export default class Humedad {
  constructor({ id_humedad, dispositivo, valor, fecha }) {
    this.id_humedad = id_humedad;
    this.dispositivo = dispositivo;
    this.valor = valor;
    this.fecha = fecha;
  }
}

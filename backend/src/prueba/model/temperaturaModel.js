export default class Temperatura {
  constructor({ id_temperatura, dispositivo, valor, fecha }) {
    this.id_temperatura = id_temperatura;
    this.dispositivo = dispositivo;
    this.valor = valor;
    this.fecha = fecha;
  }
}
